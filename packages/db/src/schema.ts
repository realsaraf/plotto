/**
 * Drizzle schema for Plotto.
 * Full table definitions land in Phase 1. This placeholder keeps the package
 * buildable so the monorepo wiring can be verified end-to-end in Phase 0.
 */

export const PLACEHOLDER = 'plotto-db-schema-v0' as const;
